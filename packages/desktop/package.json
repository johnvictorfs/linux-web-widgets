{
  "name": "@local/desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "sleep 3 && nodemon --exec 'cargo run' -e rs"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
